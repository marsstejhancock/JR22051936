{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Use appsettings to store app variables where possible like below. Default values in this code sample is for DEMO excercise only
  "APIInfo": {
    "URI": "http://numbersapi.com/{number}/{type}",
    "Name": "Fun with numbers!"
  }
}
